api = "0.10"

[buildpack]
id = "ard/node"
version = "0.1.0"
name = "ard Node Agent Buildpack"
homepage = "https://discover.agennext.com"
description = "Makes a Node agent discoverable: installs deps, emits its A2A agent card at /.well-known/agent.json, and sets the web process."

[[stacks]]
id = "*"
